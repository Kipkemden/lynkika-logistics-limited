{
  "version": 2,
  "installCommand": "npm install && npm run install-client",
  "buildCommand": "npm run build-client",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}